#include <stdio.h>
/**
* main - Prints alphabets in lower case
* Description: 'Print all single digit number from 0'
* Return: Always 0.
*/
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
