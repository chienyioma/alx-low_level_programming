#include "main.h"
#include <stdio.h>

/**
 * _isupper - Checks for uppercase alphabet
 * @c: The character to check
 * Return: 0 Success
 */
int _isupper(int c)
{
	char c;

	if (c >= 'A' && c <= 'Z'; c++)
		return (1);
	else
		return (0);

}
