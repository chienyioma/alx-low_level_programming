#include <stdio.h>

/**
 * main - Prints prime numbers
 * Return: Always 0
 */
int main(void)
{
	long int y = 612852475143;
	long int pn;

	for (pn = 2; pn <= y; pn++)
	{
		if (x % pn == 0)
		{
			i /= pn;

		}

	}
	printf("%1d\n", pn);

	return (0);

}
